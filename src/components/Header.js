import React from 'react';

export default function Header() {
    return (
        <nav className="flex flex-col content-center w-full px-6 py-2 font-sans text-center bg-white shadow bg-blue700 h-14 ">
            <button className="ml-2 text-3xl font-semibold text-gray-50 hover:text-white">
                FYRIRTÆKJALEIT
            </button>
        </nav>
    );
}
